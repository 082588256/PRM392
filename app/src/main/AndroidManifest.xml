<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Quyền sử dụng internet, mạng và vị trí -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"  android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectPRM"
        tools:targetApi="31">

        <!-- Auth Activities -->
        <activity
            android:name=".activity.auth.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.auth.SignupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <!-- Student Activities -->
        <activity android:name=".activity.student.InternshipListActivity" android:exported="false" />
        <activity android:name=".activity.student.InternshipDetailActivity" android:exported="false" />
        <activity android:name=".activity.student.ApplyActivity" android:exported="false" />
        <activity android:name=".activity.student.AppliedJobsActivity" android:exported="false" />
        <activity android:name=".activity.common.ChatActivity" android:exported="false" android:windowSoftInputMode="adjustResize" />

        <!-- Recruiter Activities -->
        <activity android:name=".activity.recruiter.PostedInternshipsActivity" android:exported="false" />
        <activity android:name=".activity.recruiter.HistoryActivity" android:exported="false" />
        <activity android:name=".activity.recruiter.AddInternshipActivity" android:exported="false" />
        <activity android:name=".activity.recruiter.RecruiterInternshipDetailActivity" android:exported="false"/>
        <activity android:name=".activity.recruiter.RecruiterChatsActivity" android:exported="false"/>

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${API_KEY}"/>

        <!-- Map hiển thị chi tiết vị trí internship -->
        <activity android:name=".activity.common.MapActivity"/>

        <!-- Activity chọn vị trí trên bản đồ (dùng cho thêm Internship mới) -->
        <activity android:name=".activity.common.PickLocationMapActivity" android:exported="false" />

        <activity android:name=".activity.common.NotificationActivity" android:exported="false" />

    </application>
</manifest>